import { FC } from 'react';
import { Typography } from 'antd';
type Props = {
  
}

const MainPage:FC<Props> = () => {
  return(
    <div>
      <Typography.Title level={2}>Main Page</Typography.Title>
    </div>
  )
}

export default MainPage;